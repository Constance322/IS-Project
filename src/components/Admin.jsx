import React from 'react';

const Admin = ({ name }) => {
  return (
    <div>
      <h2>{`${name}, welcome to the Admin page!`}</h2>
      {/* Add any additional content for the Admin page */}
    </div>
  );
};

export default Admin;
