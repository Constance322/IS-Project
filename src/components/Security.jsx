import React from 'react';

const Security = ({ name }) => {
  return (
    <div>
      <h2>{`${name}, welcome to the Security page!`}</h2>
      {/* Add any additional content for the Security page */}
    </div>
  );
};

export default Security;
